# API env (Supabase/Postgres)
# DATABASE_URL: migrations/seed/admin connection string
DATABASE_URL="postgresql://postgres:<PASSWORD>@<HOST>:5432/postgres?sslmode=require"

# APP_DATABASE_URL: runtime connection string using app_user so RLS is enforced
APP_DATABASE_URL="postgresql://app_user:<PASSWORD>@<HOST>:5432/postgres?sslmode=require"

# Optional
# PORT=3001

# Optional (AI descriptions)
# If set, the product page can generate a richer description on demand.
# OPENAI_MODEL defaults to gpt-4o-mini.
# OPENAI_API_KEY="..."
# OPENAI_MODEL="gpt-4o-mini"

# DEV ONLY (avoid in production):
# If you see TLS errors like "self-signed certificate in certificate chain" with Node/pg,
# set this to 1 to disable TLS verification.
# Prefer using Node LTS (20/22) or configuring proper CA certs instead.
# PG_TLS_INSECURE=1
